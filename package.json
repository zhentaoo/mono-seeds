{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run dev --parallel",
    "tag": "lerna version",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "qiankun": "^2.3.6"
  }
}
